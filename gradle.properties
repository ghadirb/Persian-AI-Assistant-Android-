# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx3072m -Dfile.encoding=UTF-8

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true

# Disable Gradle Configuration Cache (can cause issues in CI)
org.gradle.configuration-cache=false

# Enable Build Cache
org.gradle.caching=true

# Network optimization for Iranian users
# Increase timeout for slow connections
org.gradle.workers.max=4
systemProp.org.gradle.internal.http.connectionTimeout=60000
systemProp.org.gradle.internal.http.socketTimeout=60000

# Enable offline mode if needed (uncomment when internet is very slow)
# org.gradle.offline=true

# Disable proxy settings that might interfere with downloads
systemProp.http.proxyHost=
systemProp.http.proxyPort=
systemProp.https.proxyHost=
systemProp.https.proxyPort=
systemProp.http.nonProxyHosts=*
systemProp.https.nonProxyHosts=*

# JVM arguments for better performance with proxy disabling
org.gradle.jvmargs=-Xmx3072m -Dfile.encoding=UTF-8 -XX:+UseParallelGC -XX:MaxMetaspaceSize=512m -Djava.net.useSystemProxies=false -Dhttp.proxyHost= -Dhttp.proxyPort= -Dhttps.proxyHost= -Dhttps.proxyPort=

# Compose Compiler Version
compose_version=1.5.4

# Navigation Version
nav_version=2.5.3

# Hilt Version
hilt_version=2.44

# Room Version
room_version=2.5.2

# Core Android Libraries
core_ktx_version=1.12.0
appcompat_version=1.6.1
material_version=1.10.0
material3_version=1.1.2
lifecycle_version=2.7.0
activity_compose_version=1.8.1
compose_bom_version=2023.10.01
biometric_version=1.1.0
fragment_version=1.6.2
work_version=2.9.0
datastore_version=1.1.1

# Signing Configuration (برای CI/CD)
# این مقادیر در محیط CI/CD تنظیم می‌شوند
KEYSTORE_PATH=
KEYSTORE_PASSWORD=
KEY_ALIAS=
KEY_PASSWORD=

# Codemagic Configuration
android.enableJetifier=true
android.suppressUnsupportedCompileSdk=34
